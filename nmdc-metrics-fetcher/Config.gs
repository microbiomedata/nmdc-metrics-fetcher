const config = {
    // This is the URL of the Google Sheets document in which the
    // Google Apps Script script will store the metrics data it fetches.
    GOOGLE_SHEETS_DOCUMENT_URL: "__REPLACE_ME__",
};